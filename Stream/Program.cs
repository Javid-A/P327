using System.Text;

namespace Stream
{
    internal class Program
    {
        static void Main(string[] args)
        {
            //string desktop = @"C:\Users\Lenovo\Desktop";
            //string p327 = Path.Combine(desktop, "P327"); /*@"C:\Users\Lenovo\Desktop\P327\Inside\Nested*/
            //try
            //{
            //DirectoryInfo info = Directory.CreateDirectory(nestedPath);
            //}
            //catch (Exception ex)
            //{
            //    Console.WriteLine(ex.Message);
            //}


            //Directory.Delete(p327,true);

            //bool result = Directory.Exists(p327);

            //if (result)
            //{
            //    Directory.Delete(p327, true);
            //}
            //Console.WriteLine(result);

            //string filePath = Path.Combine(desktop, "P327", "data.txt");
            //File.Create(filePath);
            //File.WriteAllText(filePath,"This is test message");


            //File.AppendAllText(filePath,"This is test message");


            //StreamWriter writer = new StreamWriter(filePath,true);
            //for (int i = 0; i < 100; i++)
            //{
            //    writer.WriteLine(i);
            //}
            //writer.Flush();
            //writer.Close();

            //StreamReader reader = new StreamReader(filePath);
            //string line = reader.ReadToEnd();

            //Console.WriteLine(line);
            //reader.Close();

            //using (StreamReader reader = new StreamReader(filePath))
            //{
            //    Console.WriteLine(reader.ReadToEnd());
            //}

            //using (FileStream stream = new FileStream(filePath,FileMode.Truncate))
            //{
            //    string text = "My text with FileStream";
            //    byte[] bytes = Encoding.UTF8.GetBytes(text);

               
            //}
            
        }
    }
}